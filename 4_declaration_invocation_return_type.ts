// --- Declaration ---
function addNumbers(a: number, b: number): number {  
    
    return a + b;  // return 
}

// --- Invocation ---
let result = addNumbers(10, 15);
console.log("Result:", result);

// --- Return type ---
// The function gives back a "number" (25 in this case).
